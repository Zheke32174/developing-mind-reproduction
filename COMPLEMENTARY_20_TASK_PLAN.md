# The 20-Task Complementary Blueprint (Foreground Ecosystem Hardening)

This plan is executed in the foreground to bolster the background Ralph Loop (which is currently processing the 100-Task Knowledge Extraction). This ensures maximum synergy without cross-loop interference.

## Phase 1: Subconscious Daemon & Monitoring
- [ ] 1. Implement `disk_monitor.sh` (Trigger for the 15GB Blueprint).
- [ ] 2. Implement `ram_monitor.sh` (OOM prevention for paper extraction).
- [ ] 3. Create the `subconscious-daemon.sh` orchestrator.
- [ ] 4. Implement `ralph_dashboard.py` to track the background loop state.
- [ ] 5. Integrate the 30-minute checkpoint script into the daemon.

## Phase 2: Ecosystem Dependency Optimization (Prep for 15GB Blueprint)
- [ ] 6. Map all `node_modules` directories across MCP extensions.
- [ ] 7. Draft a unified `pnpm-workspace.yaml` architecture.
- [ ] 8. Create `dependency_analyzer.js` to find redundant packages.
- [ ] 9. Generate the `DEPENDENCY_CONSOLIDATION.md` report.
- [ ] 10. Stage the `pnpm` migration scripts for future Swarm execution.

## Phase 3: Cognitive Acceleration & Testing Scaffold
- [ ] 11. Create a universal `test_runner.py` in `tests/` (Offloading Task 1.5 from Ralph).
- [ ] 12. Scaffold test fixture directories for the 8 new papers.
- [ ] 13. Implement `ast_validator.py` to ensure Ralph's code is syntactically sound.
- [ ] 14. Integrate AST validation into `substrate-sync.sh`.
- [ ] 15. Create a mock environment file for isolated test runs.

## Phase 4: SwarmMail & Cross-Agent Communications
- [ ] 16. Initialize the `SwarmMail` inbox/outbox directory structure.
- [ ] 17. Write `send_swarm_alert.py` for system-level notifications.
- [ ] 18. Write `receive_swarm_alert.py` for agent interruptions.
- [ ] 19. Document the SwarmMail protocol in `ECOSYSTEM.md`.
- [ ] 20. Perform a GGA-Gated Substrate Sync of all complementary tools.
